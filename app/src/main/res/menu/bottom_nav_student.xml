<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/homeFragment"
        android:title="Home"
        android:icon="@android:drawable/ic_menu_compass" />

    <item android:id="@+id/searchFragment"
        android:title="Search"
        android:icon="@android:drawable/ic_menu_search" />

    <item android:id="@+id/favoritesFragment"
        android:title="Favorites"
        android:icon="@drawable/ic_favorite" />

    <!-- NEW — moved out of Profile into its own tab: a time-sensitive
         escrow follow-up (48hr confirm/reject window) doesn't belong
         buried in a settings-style screen. -->
    <item android:id="@+id/myReservationsFragment"
        android:title="Reservations"
        android:icon="@android:drawable/ic_menu_my_calendar" />

    <item android:id="@+id/profileFragment"
        android:title="Profile"
        android:icon="@android:drawable/ic_menu_myplaces" />
</menu>